services:
  - type: web
    name: voctrainer-app
    runtime: static
    buildCommand: "" # No build step required for raw static html/css/js
    staticPublishPath: "." # Render the root directory (containing index.html, style.css, app.js)
